#include <iostream>

// implementing the trees
/*struct TreeNode {
	object element; // the data in the node
	TreeNode* firstChild; //points to the leftmost child

	TreeNode* nextSibling;//points to the right sibling
};*/
// N = 2^(h-+1) -1 N= node , h = height , h= O(logN)
// 
// **** If the conditions holds for all the nodes, than the tree is binary-search tree******
// 
// The values of all nodes in the left sub-tree are less than X
// The values of all nodes in the right sub-tree are larger than X

// Operations on BST's
//***Implementing BTS*****
